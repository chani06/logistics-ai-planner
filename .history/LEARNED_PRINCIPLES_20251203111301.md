# การเรียนรู้จากไฟล์ Punthai Maxmart - สรุปหลักการทั้งหมด

## 📊 ข้อมูลที่วิเคราะห์
**ไฟล์:** แผนงาน Punthai Maxmart รอบสั่ง 24หยิบ 25พฤศจิกายน 2568 To.เฟิ(1) - สำเนา.xlsx
- **จำนวนเที่ยว:** 59 เที่ยว
- **จำนวนสาขา:** 503 สาขา (ไม่รวม DC011, PTDC, PTG Distribution Center)
- **จำนวนสาขาที่เรียนรู้ข้อจำกัด:** 406 สาขา

---

## 🎯 หลักการที่ 1: การจัดกลุ่มตามที่ตั้ง (Location-Based Grouping)

### สถิติจากไฟล์ Punthai:
- **จัดกลุ่มจังหวัดเดียวกัน:** 67.8% (40 เที่ยว)
- **จัดกลุ่มข้ามจังหวัด:** 32.2% (19 เที่ยว)
- **จำนวนสาขาเฉลี่ย/เที่ยว:** 8.5 สาขา
- **กลุ่มตำบลที่พบ:** 22 กลุ่ม

### การนำไปใช้:
```python
# ตรวจสอบว่าเที่ยวนี้อยู่ในจังหวัดเดียวกันหรือไม่
provinces = set(trip_data['จังหวัด'].dropna().tolist())
same_province = len(provinces) == 1

# เป้าหมาย: 67.8% ของเที่ยวควรอยู่ในจังหวัดเดียวกัน
TARGET_SAME_PROVINCE_PCT = 67.8
TARGET_BRANCHES_PER_TRIP = 9  # เรียนรู้จากค่าเฉลี่ย 8.5
```

### ลำดับความสำคัญในการจัดกลุ่ม:
1. **ตำบลเดียวกัน** (หากมีสาขาพอ)
2. **อำเภอเดียวกัน** (ตำบลต่างกันได้)
3. **จังหวัดเดียวกัน** (อำเภอต่างกันได้)
4. **ข้ามจังหวัด** (เฉพาะกรณีจำเป็น เช่น มีสาขาน้อย หรือใกล้กันมาก)

---

## 🚚 หลักการที่ 2: การเลือกรถตามข้อจำกัดสาขา (Branch Vehicle Restrictions)

### สถิติข้อจำกัดรถ:
- **สาขาที่มีข้อจำกัดเข้มงวด:** 405 สาขา (99.8%)
- **สาขาที่ยืดหยุ่น:** 1 สาขา (0.2%)

### การแบ่งตามประเภทรถ:
| ประเภทรถ | จำนวนสาขา | % | ระยะทาง (กม.) | หมายเหตุ |
|---------|-----------|---|--------------|----------|
| **4W** (รถเล็ก) | 123 | 30.3% | 41.6-471.2 (เฉลี่ย 93) | สาขาที่รถใหญ่เข้าไม่ได้ |
| **JB** (รถกลาง) | 127 | 31.3% | 13-292 (เฉลี่ย 88) | สาขาที่ไม่มี 6W |
| **6W** (รถใหญ่) | 156 | 38.4% | 1.7-513.5 (เฉลี่ย 304) | สาขาใหญ่/ไกล ต้องใช้ 6W |

### ⚠️ การค้นพบสำคัญ:
**ระยะทาง ≠ ประเภทรถ**
- สาขาห่าง 1.7 กม. ใช้ 6W (น้ำหนักมาก)
- สาขาห่าง 471 กม. ใช้ 4W ได้ (น้ำหนักน้อย + รถใหญ่เข้าไม่ได้)
- สาขาแต่ละแห่งมีข้อจำกัดเฉพาะตัว (พื้นที่จำกัด, ความกว้างถนน, etc.)

### การนำไปใช้:
```python
# ตรวจสอบรถที่สาขารองรับ
def check_branch_vehicle_compatibility(branch_code, vehicle_type):
    restrictions = PUNTHAI_PATTERNS['branch_restrictions']
    if branch_code in restrictions:
        allowed = restrictions[branch_code]['allowed']
        return vehicle_type in allowed
    return True  # ไม่มีข้อมูล = ยืดหยุ่น

# ดึงรถใหญ่สุดที่สาขารองรับ
def get_max_vehicle_for_branch(branch_code):
    restrictions = PUNTHAI_PATTERNS['branch_restrictions']
    if branch_code in restrictions:
        return restrictions[branch_code]['max_vehicle']
    return '6W'  # ไม่มีข้อมูล = ใช้รถใหญ่ได้
```

### ตัวอย่างข้อจำกัด:
```python
# GP00: สาขาเล็ก รถใหญ่เข้าไม่ได้
{
    'max_vehicle': '4W',
    'allowed': ['4W']  # ใช้ได้แค่ 4W
}

# N537: สาขายืดหยุ่น
{
    'max_vehicle': '6W',
    'allowed': ['6W', '4W']  # ใช้ได้ทั้ง 6W และ 4W
}
```

---

## 📏 หลักการที่ 3: การคำนวณระยะทางเส้นทาง (Route Distance)

### วิธีคำนวณที่ถูกต้อง:
```python
# ❌ ผิด: รวมระยะทางจาก DC ไปแต่ละสาขา
total_distance = sum(distance_from_dc(branch) for branch in branches)

# ✅ ถูก: คำนวณระยะทางเส้นทางจริง
total_distance = 0
# DC → สาขาแรก
total_distance += distance(DC, branch[0])
# สาขา → สาขา
for i in range(len(branches) - 1):
    total_distance += distance(branch[i], branch[i+1])
# สาขาสุดท้าย → DC
total_distance += distance(branch[-1], DC)
```

### กฎระยะทาง:
- **< 100 กม.:** ใช้รถเล็ก (4W/JB) ได้ (หากสาขารองรับ)
- **≥ 100 กม.:** พิจารณาใช้ 6W (ประหยัดต้นทุน เหมาะกับระยะไกล)

---

## ⚖️ หลักการที่ 4: การใช้ประโยชน์รถ (Vehicle Utilization)

### เป้าหมาย:
- **ช่วงเหมาะสม:** 90-105%
- **เป้าหมายหลัก:** 100% (ใช้พื้นที่/น้ำหนักเต็มที่)
- **ห้ามเกิน:** 105% (ล่วงหน้าไม่เกิน 5%)

### การเลือกรถ:
```python
# เลือกรถที่ใกล้ 100% มากที่สุด
distance_from_100 = abs(100 - utilization)
best_truck = min(trucks, key=lambda t: distance_from_100)

# ตัวอย่าง:
# 4W: 95% utilization → distance = 5
# JB: 85% utilization → distance = 15
# 6W: 60% utilization → distance = 40
# → เลือก 4W (ใกล้ 100% ที่สุด)
```

### การอัพเกรดรถอัตโนมัติ:
```python
if utilization > 105:
    # อัพเกรด 4W → JB → 6W
    if current_vehicle == '4W':
        if can_use_jb and jb_utilization <= 105:
            upgrade_to = 'JB'
        else:
            upgrade_to = '6W'
    elif current_vehicle == 'JB':
        upgrade_to = '6W'
```

---

## 🔄 หลักการที่ 5: การรวมเที่ยว (Trip Merging)

### เงื่อนไขการรวม (ตามลำดับความสำคัญ):

#### 1. เที่ยวเล็กมาก (≤3 สาขา)
```python
if trip1.branches <= 3 and combined_utilization <= 105:
    merge()  # รวมได้เลย เพื่อลดจำนวนเที่ยว
```

#### 2. ทั้งสองเที่ยวใช้รถน้อยมาก (<50%)
```python
if trip1.utilization < 50 and trip2.utilization < 50:
    if 60 <= combined_utilization <= 105:
        merge()  # เพิ่มประสิทธิภาพ
```

#### 3. รวมได้ใกล้ 100%
```python
if combined_branches <= 13:  # ไม่เกินจำนวนสาขาสูงสุด
    if 80 <= combined_utilization <= 105:
        merge()  # เหมาะสมมาก!
```

#### 4. รวมได้พอดี
```python
if combined_branches <= 10:
    if 70 <= combined_utilization <= 105:
        merge()  # ยอมรับได้
```

### ข้อจำกัดการรวม:
- ต้องอยู่ในจังหวัดเดียวกัน (หรือใกล้กันมาก)
- **ต้องเคารพข้อจำกัดรถ** (ไม่รวมเที่ยวที่สาขาต้องใช้รถต่างกัน)

---

## 🎯 หลักการที่ 6: การจำกัดจำนวนสาขา

### ค่าที่กำหนด:
```python
MAX_BRANCHES_PER_TRIP = 13  # สูงสุด (มีความยืดหยุ่น)
TARGET_BRANCHES_PER_TRIP = 9  # เป้าหมาย (เรียนรู้จาก Punthai 8.5)
```

### การใช้งาน:
- **เป้าหมาย:** 9 สาขา/เที่ยว (ตาม Punthai)
- **สูงสุด:** 13 สาขา/เที่ยว (กรณีรถเล็กมีสาขาใกล้กัน)
- **ยืดหยุ่น:** หากสาขาอยู่ใกล้กันมาก (ตำบล/อำเภอเดียวกัน) รับได้มากกว่า 9

---

## 🔗 การบูรณาการทั้งหมด (Full Integration)

### ขั้นตอนการวางแผนเที่ยว:

```python
def plan_trip(branches):
    # 1. จัดกลุ่มตามตำบล/อำเภอ/จังหวัด (67.8% same province)
    groups = group_by_location(branches)
    
    # 2. สร้างเที่ยวเบื้องต้น (เป้าหมาย 9 สาขา/เที่ยว)
    trips = []
    for group in groups:
        while group:
            trip = create_initial_trip(group, target_branches=9)
            trips.append(trip)
    
    # 3. เลือกรถตามข้อจำกัดสาขา + น้ำหนัก/ลูกบาศก์
    for trip in trips:
        # ดึงข้อจำกัดสาขา
        max_vehicle = get_max_vehicle_for_branches(trip.codes)
        
        # เลือกรถที่เหมาะสม (เป้าหมาย 90-105%)
        vehicle = suggest_truck(
            trip.weight, 
            trip.cube,
            max_allowed=max_vehicle,
            trip_codes=trip.codes
        )
        trip.vehicle = vehicle
        
        # อัพเกรดหากเกิน 105%
        if trip.utilization > 105:
            trip.vehicle = upgrade_vehicle(vehicle, max_vehicle)
    
    # 4. คำนวณระยะทางเส้นทาง
    for trip in trips:
        trip.distance = calculate_route_distance(trip.branches)
        
        # บังคับ 6W หากไกล ≥100km (และสาขารองรับ)
        if trip.distance >= 100:
            max_vehicle = get_max_vehicle_for_branches(trip.codes)
            if max_vehicle == '6W' and trip.vehicle != '6W':
                trip.vehicle = '6W'
    
    # 5. รวมเที่ยวที่เล็กหรือใช้รถน้อย
    trips = merge_underutilized_trips(trips)
    
    return trips
```

---

## 📈 ผลลัพธ์ที่คาดหวัง

### ตัวชี้วัดความสำเร็จ:
- ✅ **67.8%** ของเที่ยวอยู่ในจังหวัดเดียวกัน
- ✅ **90-105%** utilization (เป้าหมาย 100%)
- ✅ **0%** overload >105%
- ✅ **0%** สาขาได้รถที่เข้าไม่ได้ (เคารพข้อจำกัด)
- ✅ **9 สาขา/เที่ยว** โดยเฉลี่ย (ไม่เกิน 13)
- ✅ **ระยะทางเส้นทางถูกต้อง** (DC→branches→DC)

### ตัวอย่างผลลัพธ์:
```
เที่ยวที่ 1 (4W): 9 สาขา, จังหวัดเดียวกัน
  - น้ำหนัก: 2,300 kg (92% ของ 4W)
  - ลูกบาศก์: 4.5 m³ (90% ของ 4W)
  - ระยะทาง: 85 km
  - สาขา: GP00, O047, N614... (ทุกสาขารองรับ 4W)
  ✅ ผ่าน

เที่ยวที่ 2 (6W): 8 สาขา, ข้ามจังหวัด
  - น้ำหนัก: 5,500 kg (95% ของ 6W)
  - ลูกบาศก์: 20 m³ (91% ของ 6W)
  - ระยะทาง: 280 km
  - สาขา: N537, M036, P036... (ทุกสาขารองรับ 6W)
  ✅ ผ่าน
```

---

## 🚨 ข้อควรระวัง

### 1. สาขาที่มีข้อจำกัดเข้มงวด
```python
# หากสาขาใช้ได้แค่ 4W แต่น้ำหนักเกิน
# ระบบจะเตือน OVERLOAD แต่ยังแนะนำ 4W
# (ไม่สามารถอัพเกรดได้เพราะรถใหญ่เข้าไม่ได้)
if branch_max == '4W' and weight > 2500:
    warning = "⚠️ สาขานี้ใช้ได้แค่ 4W แต่น้ำหนักเกิน"
```

### 2. การรวมเที่ยวข้ามประเภทรถ
```python
# ห้ามรวมเที่ยวที่สาขาต้องใช้รถต่างกัน
trip1_max = '4W'  # สาขาใช้ได้แค่ 4W
trip2_max = '6W'  # สาขาต้องใช้ 6W
# → ไม่สามารถรวมได้!
```

### 3. ระยะทาง vs ประเภทรถ
```python
# ไม่ใช้กฎตายตัว "ไกล = ต้อง 6W"
# ต้องดูข้อจำกัดสาขาเป็นหลัก
if distance >= 100 and branch_allows_6w:
    consider_6w = True
else:
    use_smaller_vehicle = True
```

---

## 📝 สรุป

ระบบได้เรียนรู้และบูรณาการหลักการทั้ง 6 ข้อจาก Punthai Maxmart:

1. ✅ **Location Grouping** - 67.8% same province, 9 branches/trip
2. ✅ **Branch Vehicle Restrictions** - 405 strict restrictions (4W/JB/6W)
3. ✅ **Route Distance** - DC→branches→DC (not sum)
4. ✅ **Vehicle Utilization** - 90-105% target (auto-upgrade >105%)
5. ✅ **Trip Merging** - Smart merge underutilized trips
6. ✅ **Branch Limits** - Max 13, target 9 branches/trip

**ผลลัพธ์:** ระบบสามารถวางแผนเที่ยวที่เคารพข้อจำกัดจริง มีประสิทธิภาพสูง และสามารถนำไปใช้งานได้จริง
