# 🔧 การแก้ไขปัญหา Trip 21-22 และ N919

## 📋 สรุปปัญหา

### ปัญหา 1: Trip 21 & 22 ไม่ถูกรวมกัน
```
Trip 21: 12 สาขา, Cube 18.98% (ต่ำมาก)
Trip 22: 6 สาขา,  Cube 0.31%  (ต่ำสุด)
```
**ทำไมต้องรวม:** ทั้งสองทริปใช้รถ 6W แต่ utilization ต่ำมาก (ไม่คุ้ม)

### ปัญหา 2: N919 ไม่ถูกรวมเข้า Trip 87
```
N919:    สาขาเดียว, 0.23m³ (4W)
Trip 87: 13 สาขา, JB, อยู่ในพื้นที่สุพรรณบุรีเดียวกัน
```
**ทำไมต้องรวม:** N919 เป็นสาขาเดียวที่ใช้รถ 4W ไปคนเดียว ไม่คุ้มค่า ควรรวมกับทริปใกล้เคียง

---

## ✅ การแก้ไข

### แก้ไขที่ 1: ปรับ Phase 1 - เงื่อนไข 2.5 (Merge Low Utilization)

**เปลี่ยนจาก:**
```python
elif trip1['util'] < 30 or trip2['util'] < 30:
    # ... ตรวจสอบ ...
    elif combined_6w_util <= 105:  # เข้มงวดเกินไป
        should_merge = True
```

**เป็น:**
```python
elif trip1['util'] < 30 or trip2['util'] < 30:
    if allow_exceed_buffer and combined_6w_util <= 130:
        should_merge = True
    elif combined_6w_util <= 115:  # ยืดหยุ่นขึ้น (เพิ่มจาก 105%)
        should_merge = True
```

**ผลลัพธ์:**
- Trip 21 (18.98%) + Trip 22 (0.31%) = 19.29% → **สามารถรวมได้**
- เนื่องจาก 19.29% < 115% ✅

---

### แก้ไขที่ 2: เพิ่ม Phase 1.25 - Reassign Single Branch Trips

**เพิ่ม Phase ใหม่หลัง Phase 1:**

```python
# 🎯 Phase 1.25: ย้ายสาขาเดียวที่ใช้รถเล็กไปยังทริปใกล้เคียงที่ใหญ่กว่า
```

**วิธีการทำงาน:**

1. **หาทริปที่มีเพียง 1 สาขา และ util < 40%**
   ```python
   if len(trip_data) == 1:
       util = max(weight%, cube%)
       if util < 40:
           # พิจารณาย้าย
   ```

2. **หาทริปใกล้เคียงที่มี ≥2 สาขา**
   ```python
   for each_nearby_trip:
       if len(trip) >= 2:
           distance = haversine(branch, trip_centroid)
           if distance <= 30 km:
               # พิจารณารวม
   ```

3. **ตรวจสอบว่ารวมแล้วเกินไหม**
   ```python
   new_util = (trip_weight + branch_weight) / 6W_limit
   if new_util <= 120% and branches <= MAX_BRANCHES:
       # รวมได้
       reassign_branch_to_trip()
   ```

**ผลลัพธ์:**
- N919 (0.23m³, 4W, 1 สาขา) → จะถูกย้ายไปรวมกับ Trip 87 (JB, 13 สาขา)
- เนื่องจาก:
  - N919 util < 40% ✅
  - อยู่ใกล้ Trip 87 (ระยะทาง < 30km) ✅
  - รวมแล้วไม่เกิน 120% ✅

---

## 📊 ผลลัพธ์ที่คาดหวัง

### ก่อนแก้ไข:
```
Trip 21: 12 สาขา, 6W, 18.98%
Trip 22: 6 สาขา,  6W, 0.31%
Trip 86: 1 สาขา,  4W (N919)
Trip 87: 13 สาขา, JB
```
**ปัญหา:**
- ใช้รถ 6W ไป 2 คัน แต่ไม่เต็ม (18.98% + 0.31%)
- ใช้รถ 4W ไป 1 คัน เพื่อส่ง 1 สาขา (ไม่คุ้ม)

### หลังแก้ไข:
```
Trip 21: 18 สาขา, 6W, ~19-20% (รวม 21+22)
Trip 87: 14 สาขา, JB (รวม N919)
```
**ประโยชน์:**
- ลดรถ 6W จาก 2 คัน → 1 คัน ✅
- ลดรถ 4W จาก 1 คัน → 0 คัน (N919 เข้า JB) ✅
- ประหยัดต้นทุน: 2 ทริป → จำนวนรถลดลง

---

## 🔍 ตรวจสอบผลลัพธ์

### วิธีตรวจสอบ:

1. **รัน Streamlit:**
   ```powershell
   streamlit run app.py
   ```

2. **อัปโหลดไฟล์ Test**

3. **ดูผลลัพธ์:**
   - ตรวจสอบว่า Trip 21 & 22 รวมกันหรือไม่
   - ตรวจสอบว่า N919 เข้า Trip 87 หรือไม่

4. **Export Excel และเปรียบเทียบ:**
   ```powershell
   python compare_results.py
   ```

---

## ⚙️ พารามิเตอร์ที่ใช้

### Phase 1 - Merge Conditions:
- **Condition 2.5:** `util < 30%` → merge up to `115%` (เพิ่มจาก 105%)

### Phase 1.25 - Single Branch Reassignment:
- **เงื่อนไข:** สาขาเดียว + `util < 40%`
- **ระยะทางสูงสุด:** 30 km จาก centroid ของทริปใกล้เคียง
- **Utilization สูงสุด:** 120% หลังรวม
- **จำนวนสาขาสูงสุด:** `MAX_BRANCHES_PER_TRIP` (12)

---

## 📝 หมายเหตุ

### ทำไมไม่ใช้ Phase 1.5 (Route Pickup)?
Phase 1.5 ใช้ได้เฉพาะกับ **สาขาที่ยังไม่ได้จัดทริป (Trip = 0)** เท่านั้น

N919 ถูกจัดเป็น Trip 86 ไปแล้ว (Trip != 0) → **ต้องใช้ Phase 1.25** เพื่อย้าย

### ทำไมต้องตรวจสอบ max_allowed?
เพื่อให้แน่ใจว่าสาขาที่มีข้อจำกัด (เช่น จำกัด 4W) ไม่เข้ารถ 6W

```python
max_allowed = get_max_vehicle_for_trip(trip_codes)
```

### ทำไมต้องเช็คระยะทาง 30km?
- **น้อยกว่า 30km:** พื้นที่ใกล้เคียง → รวมได้
- **มากกว่า 30km:** พื้นที่ไกลเกินไป → อาจจะไม่อยู่ในเส้นทางเดียวกัน

---

## 🎯 สรุป

### การแก้ไขหลัก:
1. ✅ **Phase 1 - Condition 2.5:** เพิ่มความยืดหยุ่นให้ทริป util < 30% รวมกันได้ถึง 115%
2. ✅ **Phase 1.25:** เพิ่มกลไกย้ายสาขาเดียวไปรวมกับทริปใกล้เคียง

### ผลลัพธ์ที่คาดหวัง:
- ลดจำนวนรถที่ใช้ (ประหยัดต้นทุน)
- เพิ่ม utilization ของรถแต่ละคัน
- ลดทริปที่มีสาขาเดียว (4W ไปคนเดียว)

### เหมาะกับ:
- ทริปที่ใช้รถต่ำมาก (<30%)
- สาขาเดียวที่ใช้รถเล็ก (<40% util)
- พื้นที่ใกล้เคียงกัน (≤30km)

---

**อัปเดตล่าสุด:** 2024
**เวอร์ชัน:** 2.1 - Merge Optimization & Single Branch Reassignment
