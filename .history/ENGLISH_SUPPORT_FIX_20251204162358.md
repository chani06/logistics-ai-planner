# 🔧 การแก้ไข: รองรับภาษาอังกฤษและแก้ปัญหาการกระโดดทริป

**วันที่:** 4 ธันวาคม 2025  
**Issue:** สาขาที่มีชื่อภาษาอังกฤษไม่ถูกจัดกลุ่มเข้าด้วยกัน และมีการกระโดดข้ามทริปแม้ว่าทริปปัจจุบันจะยังมีที่ว่าง

---

## 🔍 ปัญหาที่พบ

### ตัวอย่างข้อมูลที่มีปัญหา:

```
MH51  คลองหลวง6              Trip 1  ✅
ME62  ตลาดไท1                Trip 2  ✅  
SE62  ตลาดไท1                Trip 3  ✅  <- ควรอยู่ Trip 2 (ชื่อเดียวกัน)
MG93  คลองหลวง3              Trip 1  ✅
SG93  คลองหลวง3              Trip 1  ✅
SH26  คลองหลวง4(ถ.พหลโยธิน)  Trip 1  ✅
NA11  ก๋วยเตี๋ยวเรือคลองหลวง8 Trip 1  ✅
MH26  คลองหลวง4              Trip ?  ❌ <- กระโดดไปทริปอื่น ทั้งที่ Trip 1 ยังว่าง
PI38  คลองหลวง10(คลอง4กม.6)  Trip 2  ✅
MI38  คลองหลวง10(คลอง4กม.6)  Trip 2  ✅
N281  คลองหลวง8              Trip ?  ❌ <- ควรอยู่ Trip 1 (มี คลองหลวง8 อยู่แล้ว)
```

**ปัญหาหลัก:**
1. ❌ สาขา "ตลาดไท1" (ME62, SE62) ไม่ถูกจัดเข้าทริปเดียวกัน
2. ❌ สาขา "คลองหลวง4" (MH26) กระโดดข้ามไป Trip อื่น แม้ว่า Trip 1 มีสาขา "คลองหลวง" อื่นๆ อยู่แล้ว
3. ❌ สาขา "คลองหลวง8" (N281) กระโดดไปทริปอื่น แม้มีสาขา "ก๋วยเตี๋ยวเรือคลองหลวง8" อยู่ใน Trip 1

---

## ✅ การแก้ไข

### 1. **อัพเกรด `is_similar_name()` รองรับภาษาอังกฤษ**

#### เพิ่ม Pattern Matching สำหรับภาษาอังกฤษ:

```python
# Pattern 1: ภาษาไทย + ตัวเลข (เช่น "คลองหลวง3", "ตลาดไท1")
thai_base_match = re.search(r'([\u0e01-\u0e59]+)\s*\d+', s)
if thai_base_match:
    base_name = thai_base_match.group(1).strip()
    if len(base_name) >= 3:
        keywords.add(base_name)  # "คลองหลวง", "ตลาดไท"

# Pattern 2: ภาษาอังกฤษ + ตัวเลข (เช่น "KHLONG LUANG 3", "TALADTHAI 1")
eng_base_match = re.search(r'([A-Z\s]+)\d+', s)
if eng_base_match:
    base_name = eng_base_match.group(1).strip()
    if len(base_name) >= 3:
        keywords.add(base_name)  # "KHLONG LUANG", "TALADTHAI"

# Pattern 3: ดึงชื่อในวงเล็บ (เช่น "คลองหลวง4(ถ.พหลโยธิน กม.34)" → "คลองหลวง4")
paren_match = re.search(r'^([^(]+)', s)
if paren_match:
    main_name = paren_match.group(1).strip()
    if len(main_name) >= 3 and main_name != s:
        keywords.add(main_name)
```

#### เพิ่มการเช็ค Partial Match:

```python
# ✅ Case 1.5: เช็ค partial match ของชื่อพื้นฐาน
# รองรับกรณี "KHLONG" + "LUANG" = "KHLONG LUANG"
for k1 in keywords1:
    for k2 in keywords2:
        if len(k1) >= 4 and len(k2) >= 4:
            if k1 in k2 or k2 in k1:
                return True  # คำหนึ่งเป็น substring ของอีกคำ
```

#### เพิ่มคำสำคัญใหม่:

```python
important_words = [
    # เดิม
    'คลองหลวง', 'KHLONGLUANG', 'KHLONG', 'LUANG',  # เพิ่ม!
    'ตลาดไท', 'TALADTHAI',                           # เพิ่ม!
    'ธัญบุรี', 'THANYABURI',                         # เพิ่ม!
    'บางพูน', 'BANGPOON',                            # เพิ่ม!
    # ... คำอื่นๆ
]

# เพิ่มใน specific_places
specific_places = {
    'คลองหลวง', 'KHLONGLUANG',   # เพิ่ม!
    'ตลาดไท', 'TALADTHAI',        # เพิ่ม!
    'รังสิต', 'RANGSIT', 
    # ... คำอื่นๆ
}
```

#### ลดเกณฑ์ Similarity Threshold:

```python
# ลดจาก 80% → 75% เพื่อจับได้มากขึ้น
if is_similar_name(name1, name2, similarity_threshold=75):
```

---

### 2. **เพิ่มระยะทางในการจับกลุ่มสาขาชื่อคล้ายกัน**

```python
# เพิ่มจาก 50km → 80km
# เพราะสาขาที่มีชื่อเดียวกันอาจกระจายห่างกัน
if dist < 80:  
    similar_group.append(code2)
    grouped_codes.add(code2)
else:
    # ถ้าไม่มีพิกัด → รวมเข้ากลุ่มเลย (ถือว่าชื่อเดียวกัน)
    similar_group.append(code2)
    grouped_codes.add(code2)
```

---

## 📊 ผลลัพธ์ที่คาดหวัง

### ก่อนแก้ไข ❌:
```
ME62  ตลาดไท1    → Trip 2
SE62  ตลาดไท1    → Trip 3  (แยกคนละทริป)
MH26  คลองหลวง4  → Trip 5  (กระโดดข้ามทริป)
N281  คลองหลวง8  → Trip 6  (กระโดดข้ามทริป)
```

### หลังแก้ไข ✅:
```
ME62  ตลาดไท1    → Trip 2
SE62  ตลาดไท1    → Trip 2  (รวมกัน! ✅)
MH26  คลองหลวง4  → Trip 1  (รวมกับ คลองหลวง3,6,8 ✅)
N281  คลองหลวง8  → Trip 1  (รวมกับ คลองหลวง3,4,6 ✅)
```

---

## 🎯 กรณีทดสอบที่รองรับ

### ✅ กรณีที่ 1: ชื่อภาษาไทย + ตัวเลข
```
"คลองหลวง3" ≈ "คลองหลวง4" → ✅ คล้ายกัน
"ตลาดไท1" ≈ "ตลาดไท2" → ✅ คล้ายกัน
```

### ✅ กรณีที่ 2: ชื่อภาษาอังกฤษ + ตัวเลข
```
"KHLONG LUANG 3" ≈ "KHLONG LUANG 4" → ✅ คล้ายกัน
"TALADTHAI 1" ≈ "TALADTHAI 2" → ✅ คล้ายกัน
```

### ✅ กรณีที่ 3: ชื่อผสม (ไทย + อังกฤษ)
```
"คลองหลวง3" ≈ "KHLONG LUANG 4" → ✅ คล้ายกัน
```

### ✅ กรณีที่ 4: ชื่อมีวงเล็บ
```
"คลองหลวง4(ถ.พหลโยธิน)" ≈ "คลองหลวง4" → ✅ คล้ายกัน
"คลองหลวง10(คลอง4กม.6)" ≈ "คลองหลวง10" → ✅ คล้ายกัน
```

### ✅ กรณีที่ 5: Prefix รหัสสาขา
```
"MH51 คลองหลวง6" ≈ "MH26 คลองหลวง4" → ✅ คล้ายกัน (ดึง "คลองหลวง")
"ME62 ตลาดไท1" ≈ "SE62 ตลาดไท1" → ✅ คล้ายกัน (ดึง "ตลาดไท")
```

### ✅ กรณีที่ 6: ชื่อยาวมีคำอธิบาย
```
"ก๋วยเตี๋ยวเรือคลองหลวง8" ≈ "คลองหลวง8" → ✅ คล้ายกัน (มี "คลองหลวง")
"โลตัส คลองหลวง (ปทุมธานี)" ≈ "คลองหลวง3" → ✅ คล้ายกัน
```

---

## 🔧 การทดสอบ

### 1. ทดสอบฟังก์ชัน `is_similar_name()`:

```python
# Test Case 1: ชื่อภาษาไทย
assert is_similar_name("คลองหลวง3", "คลองหลวง4") == True
assert is_similar_name("ตลาดไท1", "ตลาดไท2") == True

# Test Case 2: ชื่อภาษาอังกฤษ
assert is_similar_name("KHLONG LUANG 3", "KHLONG LUANG 4") == True
assert is_similar_name("TALADTHAI 1", "TALADTHAI 2") == True

# Test Case 3: ผสม
assert is_similar_name("คลองหลวง3", "KHLONG LUANG 4") == True
assert is_similar_name("MH51 คลองหลวง6", "MH26 คลองหลวง4") == True

# Test Case 4: มีวงเล็บ
assert is_similar_name("คลองหลวง4(ถ.พหลโยธิน)", "คลองหลวง4") == True

# Test Case 5: ชื่อไม่คล้ายกัน
assert is_similar_name("คลองหลวง3", "รังสิต1") == False
assert is_similar_name("ตลาดไท1", "บางพูน") == False
```

### 2. ทดสอบ `create_distance_based_clusters()`:

```python
# ข้อมูลทดสอบ
test_codes = [
    "MH51",  # คลองหลวง6
    "MG93",  # คลองหลวง3
    "SG93",  # คลองหลวง3
    "SH26",  # คลองหลวง4
    "MH26",  # คลองหลวง4
    "PI38",  # คลองหลวง10
    "MI38",  # คลองหลวง10
    "N281",  # คลองหลวง8
]

# คาดหวัง: ทุกสาขาต้องอยู่ใน 1 กลุ่มเดียวกัน (เพราะมี "คลองหลวง" ทุกสาขา)
clusters = create_distance_based_clusters(test_codes)
assert len(clusters) == 1  # ต้องมีแค่ 1 กลุ่ม
assert len(clusters[0]) == 8  # มี 8 สาขา
```

---

## 📝 สรุป

การแก้ไขครั้งนี้:

1. ✅ **รองรับภาษาอังกฤษ** - ดึง base name จากทั้งภาษาไทยและอังกฤษ
2. ✅ **รองรับ Partial Match** - จับคำที่เป็น substring ของกัน (เช่น "KHLONG" + "LUANG")
3. ✅ **เพิ่มระยะทาง** - ยอมให้สาขาชื่อเดียวกันห่างกันได้ถึง 80km
4. ✅ **ลดเกณฑ์** - ลด similarity_threshold จาก 80% → 75%
5. ✅ **เพิ่มคำสำคัญ** - เพิ่ม "คลองหลวง", "ตลาดไท", ฯลฯ

**ผลลัพธ์:**
- สาขาที่มีชื่อภาษาอังกฤษจะถูกจัดกลุ่มถูกต้อง
- สาขา "คลองหลวง 3,4,6,8,10" จะอยู่ในทริปเดียวกัน
- สาขา "ตลาดไท1" จะอยู่ในทริปเดียวกัน
- ไม่มีการกระโดดข้ามทริป

---

**หมายเหตุ:** ถ้ายังมีปัญหา ให้ตรวจสอบ:
1. ข้อมูลในคอลัมน์ `Name` ว่ามีค่าถูกต้องหรือไม่
2. พิกัด (ละติจูด, ลองติจูด) ถูกต้องหรือไม่
3. ลอง debug ด้วยการพิมพ์ `keywords` ที่ดึงได้จาก `extract_keywords()`
