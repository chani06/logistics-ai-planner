# 🚀 ขั้นตอนการ Sync ข้อมูลจาก Google Sheets

## 📋 เตรียมความพร้อม

### 1. ไฟล์ credentials.json
ต้องมีไฟล์ `credentials.json` ในโฟลเดอร์ `app/`

**วิธีสร้าง:**
1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้าง Project ใหม่หรือเลือก Project ที่มีอยู่
3. เปิดใช้งาน **Google Sheets API**
4. สร้าง **Service Account**:
   - ไปที่ IAM & Admin → Service Accounts
   - คลิก Create Service Account
   - ตั้งชื่อ เช่น "tire-data-robot"
   - สร้าง Key (JSON) และดาวน์โหลด
5. เปลี่ยนชื่อไฟล์เป็น `credentials.json`
6. วางไฟล์ใน `c:\Users\chani\app\credentials.json`

### 2. แชร์ Google Sheets
แชร์ Google Spreadsheet ให้กับ Service Account Email:
- เปิดไฟล์ `credentials.json`
- คัดลอก email: `tire-data-robot@chanipa.iam.gserviceaccount.com`
- แชร์ Spreadsheet ให้ email นี้ (Viewer หรือ Editor)

---

## 🔄 วิธีการ Sync

### วิธีที่ 1: Auto Sync (เมื่อเปิดเว็บ)
```bash
streamlit run app.py
```
- ระบบจะ Sync อัตโนมัติทุกครั้งที่โหลดหน้าเว็บ
- ข้อมูลจะถูกบันทึกใน `branch_data.json`

### วิธีที่ 2: Manual Sync (ผ่านหน้าเว็บ)
1. เปิดเว็บ `streamlit run app.py`
2. ดูที่ส่วน **📊 ข้อมูล Master (ฐานข้อมูลสาขา)**
3. คลิกปุ่ม **🔄 Sync จาก Sheets**
4. รอ 10-20 วินาที
5. ระบบจะแสดงข้อความ "✅ Sync สำเร็จ: X สาขา"
6. หน้าจะ Refresh อัตโนมัติ

### วิธีที่ 3: Sync ด้วย Python Script
```bash
python test_sheets_sync.py
```
สคริปต์นี้จะ:
- ทดสอบการเชื่อมต่อ Google Sheets API
- แสดงรายละเอียด Spreadsheet และ Worksheets
- ดึงข้อมูลและแสดงตัวอย่าง
- บันทึกข้อมูลลง `branch_data.json`

---

## 👁️ ตรวจสอบสถานะ Sync

### ผ่านหน้าเว็บ
ดูที่ส่วน **📊 ข้อมูล Master**:

**สถานะ:**
- 🟢 **อัปเดตล่าสุด** - Sync ภายใน 5 นาที
- 🟡 **อัปเดตเมื่อสักครู่** - Sync ภายใน 1 ชั่วโมง
- 🔴 **ควร Sync ใหม่** - Sync นานกว่า 1 ชั่วโมง

**ข้อมูลที่แสดง:**
- จำนวนสาขาทั้งหมด
- มี DC วังน้อยหรือไม่ (✅/❌)
- วันที่และเวลา Sync ล่าสุด

### คลิกปุ่ม 👁️ ดูข้อมูล
จะแสดง:
- จำนวนสาขาทั้งหมด
- จำนวน DC
- จำนวนจังหวัด
- ตัวอย่าง 10 สาขาแรก (รหัส, ชื่อ, จังหวัด, อำเภอ)

---

## 📊 ข้อมูลที่ Sync จาก Google Sheets

### Spreadsheet
- **ชื่อ**: "Master แผนงาน DC วังน้อย"
- **ID**: `12DmIfECwVpsWfl8rl2r1A_LB4_5XMrmnmwlPUHKNU-o`

### Worksheet
- **ชื่อ**: "Master สาขา"
- **GID**: `876257177`

### คอลัมน์ที่ดึง
| คอลัมน์ | ใช้สำหรับ |
|---------|-----------|
| Plan Code | รหัสสาขา (Primary Key) |
| สาขา | ชื่อสาขา |
| จังหวัด | จังหวัด |
| อำเภอ | อำเภอ |
| ตำบล | ตำบล |
| ละ | Latitude (พิกัด) |
| ลอง | Longitude (พิกัด) |
| MaxTruckType | ข้อจำกัดรถ (4W, JB, 6W) |

### ข้อมูลเพิ่มเติม
- **DC วังน้อย** (8nvDC011) จะถูกเพิ่มอัตโนมัติเสมอ

---

## 💾 ไฟล์ที่เกิดขึ้น

### branch_data.json
```json
{
  "S001": {
    "Plan Code": "S001",
    "สาขา": "สาขา เชียงใหม่ หางดง",
    "จังหวัด": "เชียงใหม่",
    "อำเภอ": "หางดง",
    "ตำบล": "หางดง",
    "ละ": 18.745612,
    "ลอง": 98.923456,
    "MaxTruckType": "6W"
  },
  "8nvDC011": {
    "Plan Code": "8nvDC011",
    "สาขา": "DC วังน้อย",
    "จังหวัด": "พระนครศรีอยุธยา",
    "อำเภอ": "วังน้อย",
    "ตำบล": "พยอม",
    "ละ": 14.1793943,
    "ลอง": 100.6481489,
    "MaxTruckType": "6W"
  }
}
```

**ขนาดไฟล์:** ~2-5 MB (8,147 สาขา)

---

## ⚠️ การแก้ปัญหา

### ❌ ไม่พบ credentials.json
**ข้อผิดพลาด:**
```
❌ ไม่พบ credentials.json
```

**วิธีแก้:**
1. ตรวจสอบว่ามีไฟล์ `credentials.json` ในโฟลเดอร์ `app/`
2. ตรวจสอบชื่อไฟล์ว่าเป็น `credentials.json` (ไม่ใช่ `credentials (1).json`)
3. ตรวจสอบสิทธิ์การอ่านไฟล์

### ❌ Spreadsheet not found
**ข้อผิดพลาด:**
```
gspread.exceptions.SpreadsheetNotFound
```

**วิธีแก้:**
1. ตรวจสอบ Spreadsheet ID ว่าถูกต้อง
2. แชร์ Spreadsheet ให้กับ Service Account email
3. ตรวจสอบว่า Service Account มีสิทธิ์ Viewer หรือ Editor

### ❌ Worksheet not found
**ข้อผิดพลาด:**
```
gspread.exceptions.WorksheetNotFound
```

**วิธีแก้:**
1. ตรวจสอบชื่อ Worksheet ว่าเป็น "Master สาขา"
2. ตรวจสอบว่า Worksheet ไม่ถูกลบ
3. ใช้ GID แทนชื่อ: `876257177`

### ⚠️ Sync ช้า
**อาการ:**
- Sync ใช้เวลานาน (> 30 วินาที)

**วิธีแก้:**
1. ตรวจสอบอินเทอร์เน็ต
2. ลด worksheet_row_count ใน code
3. ใช้ Auto Sync แทน Manual Sync

### ⚠️ ข้อมูลไม่อัปเดต
**อาการ:**
- แก้ไข Google Sheets แล้วแต่ข้อมูลไม่เปลี่ยน

**วิธีแก้:**
1. คลิกปุ่ม **🔄 Sync จาก Sheets** อีกครั้ง
2. ตรวจสอบว่าแก้ Worksheet ถูกต้น ("Master สาขา")
3. Refresh หน้าเว็บ (F5)

---

## 📁 การ Upload Master Data ผ่านหน้าเว็บ (ในอนาคต)

### ขั้นตอน:
1. เปิด **Sidebar** (ซ้ายมือ)
2. คลิก **"📊 จัดการข้อมูล Master"**
3. Upload ไฟล์ Excel (.xlsx)
4. คลิก **"⬆️ อัปเดต Master Data"**
5. ระบบจะอัปเดต `branch_data.json`
6. Refresh หน้า

### ไฟล์ Excel ต้องมีคอลัมน์:
- Plan Code (รหัสสาขา)
- จังหวัด
- อำเภอ
- ตำบล
- ละ / Latitude
- ลอง / Longitude
- MaxTruckType (Optional)

---

## ✅ สรุป

| วิธี | เวลา | ใช้เมื่อ |
|------|------|----------|
| **Auto Sync** | ทันทีที่เปิดเว็บ | ต้องการข้อมูลล่าสุดเสมอ |
| **Manual Sync** | กดปุ่ม 🔄 | ต้องการ Sync เอง |
| **Python Script** | รัน test_sheets_sync.py | ทดสอบการเชื่อมต่อ |
| **Upload Excel** | Upload ผ่าน Sidebar | ไม่มี Google Sheets |

**🎯 แนะนำ:** ใช้ Auto Sync และกดปุ่ม 🔄 เมื่อต้องการอัปเดตข้อมูลทันที
