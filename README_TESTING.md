# 📊 ระบบทดสอบความถูกต้อง AI Trip Planning

## 🎯 วัตถุประสงค์
ทดสอบความถูกต้องของระบบจัดทริป AI โดยเปรียบเทียบกับแผน Punthai จริง

---

## 📁 ไฟล์ที่ต้องเตรียม

### 1. ไฟล์ข้อมูลต้นทาง (Input)
- **ไฟล์ใดก็ได้ที่มีรายการสาขาที่จะส่งของ** (Excel)
- ต้องมีคอลัมน์: `Code`, `Branch_Name`, `Province`, `Subdistrict`
- ไม่จำเป็นต้องมีคอลัมน์ `Trip` หรือ `Vehicle_Type` (AI จะสร้างให้เอง)

### 2. ไฟล์แผนอ้างอิง (Reference) - **ไม่บังคับ**
- ชื่อไฟล์: `Dc/Punthai_reference.xlsx`
- ต้องมีคอลัมน์: `Code`, `Branch_Name`, `Trip`, `Vehicle_Type`
- ใช้สำหรับเปรียบเทียบว่า AI จัดทริปถูกต้องแค่ไหน

---

## 🚀 วิธีใช้งาน

### ขั้นตอนที่ 1: รัน Streamlit
```powershell
streamlit run app.py
```

### ขั้นตอนที่ 2: อัปโหลดและ Export
1. เปิดเว็บ Streamlit (จะเปิดอัตโนมัติ)
2. อัปโหลดไฟล์ Excel ที่มีรายการสาขา
3. กด "🚀 เริ่มการจัดทริป"
4. รอจนเสร็จ (จะแสดงผลทริปทั้งหมด)
5. กด "📥 Download Excel" เพื่อ Export ผลลัพธ์
6. บันทึกไฟล์เป็น: `Dc/AI_Result_Output.xlsx`

### ขั้นตอนที่ 3: วิเคราะห์ผลลัพธ์
```powershell
# ดูข้อมูลเบื้องต้น
python test_accuracy.py

# เปรียบเทียบกับ Punthai (ถ้ามีไฟล์ reference)
python compare_results.py
```

---

## 📊 ตัวชี้วัดความถูกต้อง (Metrics)

### 1. **Trip Matching Accuracy** (%)
- **คำอธิบาย**: สาขาที่ควรอยู่ทริปเดียวกัน อยู่ด้วยกันจริงหรือไม่
- **สูตร**: (จำนวนคู่ที่ถูกต้อง) / (จำนวนคู่ทั้งหมดใน Punthai)
- **เป้าหมาย**: ≥ 80%

### 2. **Vehicle Assignment Accuracy** (%)
- **คำอธิบาย**: รถที่เลือกใช้ตรงกับ Punthai หรือไม่
- **สูตร**: (สาขาที่ใช้รถถูกต้อง) / (จำนวนสาขาทั้งหมด)
- **เป้าหมาย**: ≥ 85%

### 3. **Branch Count MAE**
- **คำอธิบาย**: ค่าเฉลี่ยความผิดพลาดจำนวนสาขาต่อทริป
- **สูตร**: Mean Absolute Error ระหว่าง AI และ Punthai
- **เป้าหมาย**: ≤ 2 สาขา

### 4. **Overall Score** (%)
- **คำอธิบาย**: คะแนนรวมจากทุกตัวชี้วัด
- **การให้เกรด**:
  - 90-100%: Excellent ⭐⭐⭐⭐⭐
  - 80-89%: Very Good ⭐⭐⭐⭐
  - 70-79%: Good ⭐⭐⭐
  - 60-69%: Fair ⭐⭐
  - <60%: Needs Improvement ⭐

---

## 📈 ตัวอย่างผลลัพธ์

```
================================================================================
📊 สรุปผลการเปรียบเทียบ
================================================================================

🔍 Trip Matching Accuracy:     85.3% ⭐⭐⭐⭐
🚛 Vehicle Assignment Accuracy: 88.7% ⭐⭐⭐⭐
📦 Branch Count MAE:            1.8 สาขา ⭐⭐⭐⭐

--------------------------------------------------------------------------------
🏆 Overall Score: 87.3% - Very Good ⭐⭐⭐⭐
--------------------------------------------------------------------------------
```

---

## 🔧 การปรับปรุงระบบ

### ถ้า Trip Matching ต่ำ (<80%)
→ ปรับ Phase 1 merge conditions ใน `app.py`
- เพิ่ม/ลดระยะทาง threshold
- ปรับเงื่อนไข small trip priority
- เพิ่มจำนวน merge iterations

### ถ้า Vehicle Accuracy ต่ำ (<85%)
→ ปรับ Phase 2 vehicle selection logic
- ตรวจสอบ Booking History data
- ปรับเงื่อนไข region-based selection
- ตรวจสอบ branch restrictions

### ถ้า Branch Count MAE สูง (>2)
→ ปรับพารามิเตอร์การจัดกลุ่ม
- เปลี่ยน MAX_BRANCHES_PER_TRIP
- ปรับ TARGET_BRANCHES_PER_TRIP
- เปลี่ยน merge distance thresholds

---

## 🐛 การแก้ปัญหา

### ปัญหา: ไม่พบไฟล์ AI_Result_Output.xlsx
**วิธีแก้**: 
1. ตรวจสอบว่า Export แล้วหรือยัง
2. ตรวจสอบว่าบันทึกในโฟลเดอร์ `Dc/` หรือไม่
3. ตรวจสอบชื่อไฟล์ว่าถูกต้อง

### ปัญหา: ไม่พบไฟล์ Punthai_reference.xlsx
**วิธีแก้**:
1. ถ้าไม่มีไฟล์ reference → ไม่ต้องกังวล (ระบบจะข้ามการเปรียบเทียบ)
2. ถ้ามีไฟล์ → ตั้งชื่อเป็น `Punthai_reference.xlsx` แล้ววางในโฟลเดอร์ `Dc/`

### ปัญหา: ValueError - Missing columns
**วิธีแก้**:
1. ตรวจสอบว่าไฟล์มีคอลัมน์ที่จำเป็น: `Code`, `Branch_Name`, `Trip`, `Vehicle_Type`
2. ตรวจสอบว่าชื่อคอลัมน์เขียนถูกต้อง (case-sensitive)

---

## 📞 ติดต่อ
หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ error message ที่แสดงในหน้าจอ
2. อ่าน README นี้อีกครั้ง
3. ตรวจสอบว่าไฟล์ทั้งหมดอยู่ในตำแหน่งที่ถูกต้อง

---

## ⚙️ ข้อมูลเทคนิค

### ระบบ AI Trip Planning
- **Algorithm**: 6-Phase Optimization
  - Phase 1: Merge Trips (centroid-based)
  - Phase 1.5: Route Pickup
  - Phase 2: Vehicle Selection (region-based)
  - Phase 2.1: Restriction Check
  - Phase 2.5: Split Overloaded
  - Phase 3: Split Underutilized

### พารามิเตอร์หลัก
- **MAX_BRANCHES_PER_TRIP**: 12
- **MAX_DETOUR_KM**: 12
- **MAX_MERGE_ITERATIONS**: 50
- **Vehicle Limits**: 4W (5m³), JB (7m³), 6W (20m³)
- **Target Cube**: 95-120%

### Performance
- **Cache TTL**: 7200 วินาที (2 ชั่วโมง)
- **Processing Time**: ~30-60 วินาที (สำหรับ 400 สาขา)

---

**อัปเดตล่าสุด**: 2024
**เวอร์ชัน**: 2.0 - Offline Testing System
